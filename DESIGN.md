# 企业管理系统 UI 设计规范

## 产品定位

面向运输、调度、财务和行政人员的桌面端业务系统。首要目标是快速定位异常、批量处理记录并保留可追溯的结果；页面优先保证信息密度、稳定布局和明确反馈，不使用营销式大图或装饰性卡片堆叠。

## 视觉与布局

- 页面底色使用 `--admin-page-bg`，业务区使用 `--admin-surface`，边框使用 `--admin-border-subtle`。
- 统一间距基线为 8px；业务卡片圆角不超过 8px，阴影只用于可交互或明确分组的卡片。
- 桌面端内容区保留侧栏和多标签页；小于 900px 时标题、筛选器和操作区允许换行，小于 640px 时将横向表格放入明确的滚动容器。
- 宽表必须提供最小列宽、固定关键列和横向滚动，不通过压缩字号解决可读性问题。

## 颜色与状态

- 主操作使用 `--admin-primary`；正文链接和趋势文本使用 `--admin-primary-text`。
- 成功、预警、危险状态同时显示文字或图标，不能只依赖颜色；状态标签优先使用明确的中文状态值。
- 统计卡在没有当前记录时显示“当前筛选范围暂无数据”，不得把空数据渲染成“0”并计算下降百分比。
- 数据源不可用时显示可重试的警告态；加载态、空态、错误态必须互相区分。

## 交互规则

- 删除、作废、锁定、付款确认等不可逆或高风险动作必须二次确认，并说明影响范围。
- 服务端拒绝写入时恢复本地乐观修改，保留用户输入并提示可执行的下一步。
- 键盘焦点使用可见的 2px 外轮廓；`prefers-reduced-motion: reduce` 时全局降低动画和过渡。
- 生产构建不显示开发设置抽屉；设置能力只用于本地开发和验收。

## 表格与响应式验收

- 默认列只展示完成当前任务所需的信息，扩展字段通过列设置或详情抽屉查看。
- 1440px、1024px、768px、560px 四个宽度至少验收登录、审批中心、工资、运输运营和系统用户页面。
- 验收项包括：无页面级横向溢出、表头与操作不重叠、筛选器可操作、按钮有焦点反馈、空态不误报趋势。
